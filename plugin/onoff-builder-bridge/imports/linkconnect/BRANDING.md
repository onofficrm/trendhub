# SPA Branding Lock (TrendHub)

`plugin/onoff-builder-bridge/imports/linkconnect/` 는 **트랜드허브 전용** SPA 입니다.

- lock 파일: `spa-brand.trendhub` (`brand=trendhub`)
- 갱신: `builder/linkconnect_source` 에서 `npm run deploy:imports`
- linkconnect / onoffcpa 저장소의 imports 를 rsync 하지 마세요.
